import 'package:flutter/material.dart';

class MColor {
  static const lightBlue = Color(0XFF0E8BE3);
  static const blue = Color(0XFF015FB1);

  static const lightGray = Color(0xFFEFEFEF);
  static const gray = Color(0xFFB0B0B0);

  static const lightBlack = Color(0xFF2B2B2B);
  static const black = Color(0xFF0E0E0E);

  static const clear = [MColor.blue, MColor.lightBlue];
  static const cloudy = [MColor.gray, MColor.lightGray];
  static const rainy = [MColor.black, MColor.lightBlack];
}
